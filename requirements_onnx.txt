# ONNX-based Face Detection Requirements
# Compatible with Python 3.10, 3.11, 3.12, 3.13+

# Core dependencies - using latest versions for Python 3.14 compatibility
onnxruntime>=1.16.0              # CPU version (will use latest available)
# onnxruntime-gpu>=1.16.0        # Uncomment for GPU support (requires CUDA)

# InsightFace - provides pre-trained ONNX models and easy API
insightface>=0.7.3

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0,<2.0.0            # Cap at <2.0 for compatibility

# Clustering
scikit-learn>=1.3.0

# Optional: For better performance
# albumentations>=1.3.1          # Image augmentation
# onnx>=1.15.0                   # ONNX model inspection tools
