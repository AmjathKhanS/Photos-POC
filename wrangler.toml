# Cloudflare Pages Configuration
# This file is optional - you can also configure everything via the Cloudflare dashboard

name = "photo-viewer"
compatibility_date = "2024-01-01"

[build]
command = "cd client && npm install && npm run build"
cwd = "."

[build.upload]
dir = "client/dist"

# Environment variables (configure these in Cloudflare dashboard instead)
# VITE_API_URL is already set in client/.env.production
